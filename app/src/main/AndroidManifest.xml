<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marce.teste">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Lista_todos" />
        <activity android:name=".Activity_Principal">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Activity_Historico" />
        <activity android:name=".Activity_Favoritos" />
        <activity android:name=".Activity_Creditos" />
        <activity android:name=".rest.Activity_Detalhes" />
        <activity android:name=".Activity_Detalhes">



        </activity>
    </application>

</manifest>